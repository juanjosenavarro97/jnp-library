# 🚀 Bienvenido a la Component Library

Esta es una **colección de componentes reutilizables** desarrollados con React, pensados para acelerar el desarrollo de aplicaciones y mantener consistencia en el diseño.

## ✨ Características

- 📦 Componentes modulares y reutilizables
- 📚 Documentados con Storybook
- 🧪 Testeados y listos para producción

## 📖 Cómo usar

Instala la librería en tu proyecto:

```bash
npm install jnp-library

# con yarn
yarn add jnp-library

# con pnpm
pnpm add jnp-library
```

Importa los componentes en tu archivo de código:

```tsx
import JnpButton from "jnp-library/components/jnp-button";

function App() {
  return <JnpButton>Botón</JnpButton>;
}
```
